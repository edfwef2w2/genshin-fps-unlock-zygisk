name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Magisk zip
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      NDK_VERSION: "27.2.12479018"
      CMAKE_VERSION: "3.22.1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK packages
        run: |
          set -euo pipefail
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager \
            "platform-tools" \
            "platforms;android-34" \
            "build-tools;34.0.0" \
            "ndk;${NDK_VERSION}" \
            "cmake;${CMAKE_VERSION}"

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon --stacktrace packageMagiskZip

      - name: Pack outputs
        run: |
          set -euo pipefail
          mkdir -p dist/module
          unzip -q out/genshin-fps-unlock-zygisk.zip -d dist/module
          rm -f dist/module/app.apk
          (cd dist/module && zip -qr ../genshin-fps-unlock-zygisk.zip .)
          rm -rf dist/module
          cp app/build/outputs/apk/release/app-release.apk dist/genshin-fps-unlock.apk
          ls -l dist/genshin-fps-unlock.apk dist/genshin-fps-unlock-zygisk.zip

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: dist/genshin-fps-unlock.apk
          if-no-files-found: error

      - name: Upload Magisk module
        uses: actions/upload-artifact@v4
        with:
          name: magisk-module
          path: dist/genshin-fps-unlock-zygisk.zip
          if-no-files-found: error

      - name: Create GitHub Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/genshin-fps-unlock.apk
            dist/genshin-fps-unlock-zygisk.zip
          generate_release_notes: true
